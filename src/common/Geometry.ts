
type Bounding = {
    x: number;
    y: number;
    width: number;
    height: number;
}

type Position = {
    x: number;
    y: number;
}

type Rect = Bounding;
type Vec2 = Position;

export {
    Bounding,
    Rect,
    Position,
    Vec2,
}